{
  "$schema": "https://react.doctor/schema/config.json",
  "ignore": {
    // nitro build output: bundled third-party deps, gitignored, regenerated by `bun run build`
    "files": [".output/**"]
  }
}
